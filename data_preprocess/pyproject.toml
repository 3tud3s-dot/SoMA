[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "soma-preprocess"
version = "0.1.0"
description = "Data preprocessing utilities for SoMA."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["preprocess*"]
